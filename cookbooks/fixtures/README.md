# Cookbook Fixtures

Place small target applications or source trees for cookbook examples under `fixtures/apps/<cookbook_slug>/`.

Fixtures must be deterministic, local-only, and free of absolute checkout paths.
