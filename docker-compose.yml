services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    environment:
      RAILS_ENV: development
      DATABASE_URL: postgres://taskrail:taskrail@postgres:5432/taskrail_development
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: taskrail
      POSTGRES_PASSWORD: taskrail
      POSTGRES_DB: taskrail_development
    ports:
      - "3333:3000"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - .:/rails
      - bundle_cache:/usr/local/bundle
      - node_modules_cache:/rails/tui/node_modules

  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: taskrail
      POSTGRES_PASSWORD: taskrail
      POSTGRES_DB: taskrail_development
    ports:
      - "5435:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U taskrail -d taskrail_development"]
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  bundle_cache:
  node_modules_cache:
  postgres_data:
