# 2026-05-15 Blockers

This file records local blockers encountered while executing the numbered 2026-05-15 specs. A blocker here means the specific issue or spec could not be completed immediately; work should continue with the next actionable item.

## Resolved

- **Local test database connection:** Resolved by moving TaskRail's local Postgres host port from `5433` to `5435`, starting `docker compose up -d postgres`, and preparing the test DB. The focused hardening suite now passes with `82 examples, 0 failures`.
- **SBOM artifact publication:** Resolved by adding a Woodpecker upload step that copies `sbom.json` to the QNAP NAS at `192.168.10.22:/share/Public/taskrail/sbom/${CI_COMMIT_SHA}/sbom.json` using the `nas_ssh_key` Woodpecker secret. Local dry-run upload to `/share/Public/taskrail/sbom/local-dry-run/sbom.json` succeeded.

## 2026-05-15-1-engine-safety

- **Item:** Verification for engine runner and claim heartbeat specs.
- **Where left:** Implemented runner locking/error isolation changes and added focused runner specs. Existing `Claim#heartbeat_stale?` already guards nil heartbeats, and the model spec already covers the nil case.
- **Blocker:** Resolved. The focused specs now pass on the TaskRail Postgres service at host port `5435`.
- **Remaining:** None for this item.

## 2026-05-15-2-database-hardening

- **Item:** Verification for database hardening migration/spec.
- **Where left:** Updated Kamal database credentials to use fail-fast `ENV.fetch`, added a reversible migration for hot-path indexes and enum check constraints, updated schema representation, and added DB specs.
- **Blocker:** Resolved. The focused specs now pass on the TaskRail Postgres service at host port `5435`.
- **Remaining:** None for this item.

## 2026-05-15-3-security-hardening

- **Item:** Verification for security hardening adapter/service/request specs.
- **Where left:** Implemented shell working-directory sandbox validation, command trace redaction, fail-closed GitHub webhook secrets, work item stage validation, tag sanitization, and expanded trace redaction. Added focused adapter, service, and request specs. Ruby syntax checks pass, and `TraceRedactor.safe_summary` was sanity-checked directly with Ruby.
- **Blocker:** Resolved. The focused specs now pass on the TaskRail Postgres service at host port `5435`.
- **Remaining:** None for this item.

## 2026-05-15-4-api-hardening

- **Item:** Verification for API pagination, request limit, and rate-limit specs.
- **Where left:** Added pagination helpers and paginated `work_items`, `pipes`, and `costs` responses; added global 1 MB body limit, field-size validation, Rack::Attack throttles, and focused request specs. `bundle install` completed and updated the lockfile for `rack-attack`. Ruby syntax checks pass.
- **Blocker:** Resolved. The focused specs now pass on the TaskRail Postgres service at host port `5435`.
- **Remaining:** None for this item.

## 2026-05-15-5-ci-hardening

- **Item:** End-to-end CI verification and SBOM artifact publication.
- **Where left:** Added gitleaks, removed the type-check placeholder, added Syft SBOM generation, and added Dockerfile `HEALTHCHECK`. `.woodpecker.yml` parses as valid YAML.
- **Blocker:** Resolved. Woodpecker now uploads `sbom.json` to the QNAP NAS after generation.
- **Remaining:** The `nas_ssh_key` secret has been added to Woodpecker for `gregmushen/stupidclaw` using a key authorized for `admin@192.168.10.22`. The NAS upload step is scoped to `push` and `manual` events so pull-request jobs do not receive the SSH key. Verify a clean push/PR pipeline end to end.

## 2026-05-15-6-test-coverage

- **Item:** Verification for the added job, adapter, and security specs.
- **Where left:** Added EngineTickJob behavior specs, CheckAsyncClaimsJob behavior specs, ShellScriptAdapter timeout/multi-command specs, InlineClaude/Codex adapter coverage, and security request scenario specs. Added async stale-heartbeat handling and shell command timeout behavior needed by those specs. Ruby syntax checks pass.
- **Blocker:** Resolved. The focused specs now pass on the TaskRail Postgres service at host port `5435`.
- **Remaining:** None for this item.
